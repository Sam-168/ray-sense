# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/attendance_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Samu@20052012
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Python Service
face.recognition.service.url=http://localhost:8000

# File Uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2MB

# JWT
jwt.secret=myNewSuperSecureSecretKeyThatIsDifferent123!
jwt.expiration=43200000

# Logging
logging.level.root=INFO
logging.level.com.attendance.facerec=INFO

# CORS
cors.allowed.origins=http://localhost:5173,http://localhost:3000